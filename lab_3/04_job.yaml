apiVersion: batch/v1
kind: Job
metadata:
  name: pod-job
  namespace: qtul
spec:
  completions: 3
  template:
    spec:
      containers:
      - name: sleepy
        image: alpine
        command: ["/bin/sleep"]
        args: ["3"]
      restartPolicy: Never
